spring.profiles.active: development
---
spring:
  profiles: development
  application:
    name: spring-boot-api
server:
  port: 8888
  servlet:
    contextPath: /api
---
spring:
  profiles: production
  application:
    name: spring-boot-api
server:
  port: 8888
  servlet:
    contextPath: /api
